import { BusUsuarioPipe } from './bus-usuario.pipe';

describe('BusUsuarioPipe', () => {
  it('create an instance', () => {
    const pipe = new BusUsuarioPipe();
    expect(pipe).toBeTruthy();
  });
});